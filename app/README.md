# App — Streamlit Dashboard

> **Scope:** Person 2 — Vectorization, ML classification, and Streamlit dashboard.

This module is **out of scope for Person 1**. Person 2 will implement:

- `app/main.py` — Streamlit dashboard entry point
- Interactive map showing change polygons overlaid on imagery
- Class labels and magnitude heatmap
- Filtering by change class and date

## Run (after Person 2 implements)

```bash
streamlit run app/main.py
```

Opens at `http://localhost:8501`.
