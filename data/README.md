# Data

This directory contains raw and processed datasets for Cipher-X.

## Structure

- `raw/` — Original, immutable data dumps
- `processed/` — Cleaned, transformed data ready for modeling

> **Note:** Do not commit large data files. Use `.gitignore` or external storage.
